# Notification Service Environment Variables

# Server port (default: 3006)
PORT=3006

# JWT secret for authentication (shared with other services)
# Used for verifying JWT tokens from cookies
JWT_SECRET=your-super-secret-jwt-key-change-in-production

NODE_ENV=development

# Redis connection
REDIS_HOST=redis
REDIS_PORT=6379 #must be 79, this is inside the container

# Presence service URL
PRESENCE_SERVICE_URL=http://presence:3005
